{
  "scripts": {
    "vercel-build": "cd frontend && npm ci --no-audit --no-fund 2>/dev/null || npm install --no-audit --no-fund && npm run build",
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "crypto-js": "^4.2.0",
    "jsdom": "^26.0.0"
  }
}
